# -------------------------------
# Frontend (Vite) Environment
# -------------------------------

# Clerk publishable key (Frontend only)
# Get it from https://dashboard.clerk.com
VITE_CLERK_PUBLISHABLE_KEY=

# Backend API base URL that the frontend should call
# For local development: http://localhost:5000
# For Vercel production: https://your-backend.vercel.app
VITE_API_URL=http://localhost:5000

# Pusher public credentials (used by the browser)
VITE_PUSHER_KEY=
VITE_PUSHER_CLUSTER=


# -------------------------------
# Backend (Node/Express) Environment
# -------------------------------

# Clerk secret key (NEVER expose to the browser)
CLERK_SECRET_KEY=

# MongoDB connection (Atlas or local)
MONGODB_URI=
MONGODB_DB=re7lty

# Pusher server credentials
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=

# Local development port for the backend
PORT=5000








