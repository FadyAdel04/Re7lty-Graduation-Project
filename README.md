# 🌍 Re7lty – شارك رحلتك | Share Your Journey

## 🧭 نظرة عامة (Overview)

**Re7lty** منصة تفاعلية تجمع بين **الشبكات الاجتماعية للسفر** و**رحلات الشركات وحجز التذاكر** و**الذكاء الاصطناعي لاقتراح الرحلات**.  
يمكن للمستخدمين مشاركة رحلاتهم، استكشاف رحلات الآخرين، ومتابعة المسافرين. كما يمكن للشركات نشر رحلاتها وحجز العملاء وإدارة الحجوزات والدفعات. ويساعد **TripAI** في تصميم رحلات مخصصة حسب الوجهة والميزانية والأيام.

---

## ✨ المحتوى الرئيسي الثلاثي (Three Main Pillars)

### 1️⃣ السوشيال ميديا للسفر (Social Media for Travel)

منصة اجتماعية مخصصة لعشاق السفر: مشاركة، استكشاف، ومتابعة.

| الميزة | الوصف |
|--------|--------|
| **التايملاين (Timeline)** | شريط رحلات منشورات المستخدمين والمتابَعين. |
| **استكشاف (Discover)** | بحث عن رحلات ومستخدمين حسب الكلمة، فلتر (ترند / حديث)، خريطة تفاعلية بنقاط الرحلات. |
| **إنشاء رحلة (Create Trip)** | إضافة وجهة، مدة، تفاصيل يومية، أماكن (مطاعم، متاحف، شواطئ)، رفع صور وفيديو، وعرض المسار على خريطة تفاعلية. |
| **عرض الرحلة على الخريطة** | خريطة تفاعلية لمسار الرحلة، ماركرات للنقاط، أنيميشن للمسار. |
| **التفاعل الاجتماعي** | إعجابات ❤️ وتعليقات 💬 على الرحلات، متابعة المسافرين 👥. |
| **صفحة المستخدم (Profile)** | عدد الرحلات، الدول التي زارها (مع خريطة)، المتابعون، مستوى "Explorer Level"، الشبكة (اتصالات). |
| **لوحة المتصدرين (Leaderboard)** | أكثر المسافرين نشاطاً وتميزاً. |
| **الذكريات (Travel Memories)** | إنشاء فيديو تلقائي من صور الرحلة (Slideshow)، إضافة موسيقى ونصوص، تصدير "Travel Memory Clip". |
| **الرسائل المباشرة (Direct Messages)** | محادثات خاصة بين المستخدمين. |
| **قصص (Story Mode)** | عرض الرحلة بأسلوب يشبه الـ Story. |
| **تقويم الرحلات والميزانية والطقس** | تقويم شخصي، تقدير ميزانية، طقس متوقع، اقتراح مواصلات محلية. |

---

### 2️⃣ الشركات ومنصة SaaS (Companies & SaaS)

إدارة شركات السياحة، رحلات الشركات، الحجوزات، الدفع، والرسائل.

| الميزة | الوصف |
|--------|--------|
| **تسجيل الشركات (Onboarding)** | اختيار دور (مستخدم / شركة)، تقديم طلب شركة، مراجعة من الإدارة. |
| **لوحة تحكم الشركة (Company Dashboard)** | واجهة واحدة للشركة: ملخص إحصائيات، رحلات، حجوزات، رسائل، مقاعد، كوبونات، تقارير، إعدادات. |
| **ملخص الأداء (Summary / KPI Cards)** | إجمالي الحجوزات، طلبات معلقة، حجوزات مقبولة، الرحلات النشطة، صافي الأرباح، أرباح الأسبوع، مدفوع (محصّل)، مطلوب تحصيله، مشاهدات الرحلات، حجوزات اليوم، أرباح الشهر. |
| **رحلات الشركة (Corporate Trips)** | إنشاء وتعديل رحلات الشركة (عنوان، وصف، تاريخ، سعر، صور)، نشرها للعملاء. |
| **صفحة تفاصيل الرحلة (Trip Details)** | عرض رحلة شركة للعميل مع حجز وتحديد مقاعد وكوبونات. |
| **الحجوزات (Bookings)** | قائمة حجوزات الشركة، قبول/رفض، تحديث حالة الدفع (مدفوع / معلق / مسترجع)، طريقة الدفع (نقدي / بطاقة / تحويل). |
| **توزيع المقاعد (Seats)** | تخطيط حافلة تفاعلي، حجز مقاعد للحجوزات. |
| **كوبونات الخصم (Coupons)** | إنشاء كوبونات، ربطها بالحجوزات. |
| **الرسائل والاستفسارات (Contact / Messages)** | استفسارات العملاء من لوحة الشركة، محادثات مباشرة مع الشركة، إشعارات فورية وعداد غير مقروء. |
| **مجموعات رحلات الشركة (Trip Group Chat)** | مجموعة دردشة لكل رحلة شركة: مسؤول الرحلة (الشركة)، المشاركون، إرسال نصوص وملفات (صور، فيديو، صوت، PDF)، ردود فعل، إعلانات، قفل المجموعة. |
| **إشعارات الشركة** | إشعارات عند رسالة جديدة (استفسار أو مجموعة رحلة)، عداد في تبويب "الرسائل". |
| **التقارير (Reports)** | تقارير وأداء الحجوزات والإيرادات. |
| **الإعدادات** | بيانات الشركة (اسم، شعار، وصف، ألوان، تواصل، عنوان). |
| **عمولة المنصة** | عمولة 5% على كل حجز ناجح (موضحة في لوحة الشركة). |
| **لوحة الإدارة (Admin)** | إدارة المستخدمين، الشركات، الرحلات، الطلبات، التقارير المالية، الشكاوى، الإبلاغات، إدارة المحتوى. |

---

### 3️⃣ اقتراح الرحلات بالذكاء الاصطناعي (AI Trip Suggestion)

مساعد ذكي لتصميم رحلات مخصصة وربطها بمنصة Re7lty.

| الميزة | الوصف |
|--------|--------|
| **صفحة TripAI (/trip-assistant)** | دردشة مع مساعد الرحلات: تحديد الوجهة، عدد الأيام، الميزانية، نوع الرحلة، الفصل. |
| **اقتراحات ذكية** | اقتراح رحلات بناءً على الموقع، الأيام، الميزانية، ونوع الرحلة (مغامرة، استجمام، عائلي، رومانسي). |
| **خطة رحلة (Trip Plan)** | توليد خطة تفصيلية: معالم، مطاعم، فنادق قابلة للتحديد وإضافتها للرحلة. |
| **ربط برحلات المنصة** | عرض رحلات من Re7lty مطابقة لمعايير المستخدم مع رابط للتفاصيل. |
| **إنشاء رحلة من الـ AI** | تحويل الخطة المُولدة إلى رحلة فعلية في المنصة (Create Trip) بنقرة واحدة. |
| **ويدجت AI عام (TripAIChatWidget)** | ويدجت دردشة AI متاح في كل الصفحات (ما عدا صفحات الإدارة والـ onboarding وصفحة المساعد نفسها) للاستفسار السريع. |
| **استكشاف وفلتر ذكي** | في صفحة Discover: بحث، فلتر ترند/حديث، خريطة حية، اقتراح رحلات ومستخدمين. |

---

## ⚙️ إعداد متغيرات البيئة

1. **الواجهة الأمامية (Vite)**  
   - انسخ الملف [`env.example`](env.example) إلى `.env` في جذر المشروع:  
     ```bash
     cp env.example .env
     ```
   - عدّل قيم `VITE_*` حسب مفاتيحك (Clerk، عنوان الـ API، Pusher، Cloudinary إن وُجد).

2. **الخلفية (Node/Express)**  
   - انسخ نفس الملف إلى داخل مجلد `backend` باسم `.env`:  
     ```bash
     cp env.example backend/.env
     ```
   - حدّث المفاتيح (مثل `CLERK_SECRET_KEY`، `MONGODB_URI`، Pusher، Cloudinary، OpenAI/OpenRouter للـ AI).

> عند النشر على Vercel، أدخل القيم في صفحة متغيرات البيئة لكل مشروع كما في قسم "نشر الـ Backend على Vercel".

---

## 🧩 التقنيات المستخدمة (Tech Stack)

| الجزء | التقنية |
|-------|-----------|
| **الواجهة الأمامية** | React, Vite, TypeScript, Tailwind CSS, Framer Motion |
| **الخلفية** | Node.js, Express |
| **قاعدة البيانات** | MongoDB (Mongoose) |
| **الخرائط** | Google Maps API / Mapbox |
| **رفع الوسائط** | Cloudinary |
| **المصادقة** | Clerk |
| **الذكاء الاصطناعي** | OpenRouter / OpenAI API (TripAI) |
| **الإشعارات والوقت الفعلي** | Pusher |
| **الاستضافة** | Vercel (Frontend & Backend Serverless) |

---

## 🌍 أماكن الرحلات في النسخة الأولى

مرسى مطروح، الإسكندرية، الساحل الشمالي، الأقصر، أسوان، الغردقة، شرم الشيخ.

---

## 🧠 فكرة المشروع (Why It’s Creative)

Re7lty ليست مجرد موقع حجز أو مدونة سفر؛ هي تجربة تفاعلية تجمع:

- **السوشيال ميديا** (مشاركة، متابعة، تعليقات، رسائل، مجموعات رحلات).  
- **SaaS للشركات** (رحلات، حجوزات، دفعات، مقاعد، كوبونات، رسائل، تقارير).  
- **الذكاء الاصطناعي** (اقتراح رحلات، خطة تفصيلية، ربط برحلات المنصة، ويدجت عام).  
- **الخرائط والوسائط** (مسار تفاعلي، صور، فيديو، ذكريات).

---

## 🚀 نشر الـ Backend على Vercel (Serverless)

1. **إعداد التطبيق**  
   - التطبيق مُستخرج في `backend/src/app.ts`؛ `backend/src/server.ts` للتشغيل المحلي.  
   - ملف Serverless في `backend/api/index.js` يصدّر التطبيق من `dist`.

2. **ضبط المشروع في Vercel**  
   - أنشئ مشروعاً جديداً واجعل **Root Directory** هو المجلد `backend`.  
   - استخدم `npm run build` لتوليد `dist/`.  
   - `backend/vercel.json` يوجّه الطلبات إلى `api/index.js`.

3. **متغيرات البيئة على Vercel**  
   - استخدم [`env.example`](env.example) كمرجع.  
   - **Frontend:** `VITE_CLERK_PUBLISHABLE_KEY`, `VITE_API_URL`, `VITE_PUSHER_KEY`, `VITE_PUSHER_CLUSTER`.  
   - **Backend:** `CLERK_SECRET_KEY`, `MONGODB_URI`, `PUSHER_*`, Cloudinary إن لزم.

4. **Pusher**  
   - إنشاء تطبيق Pusher وإضافة المفاتيح في `.env` و Vercel.  
   - البث على قنوات مثل `user-{clerkId}` و `trip-group-{groupId}`؛ الاستماع من `NotificationBell` وواجهات الدردشة.

5. **تشغيل محلي**  
   ```bash
   cd backend && npm install && npm run dev
   ```  
   ولتجربة نسخة Vercel محلياً: `npm run build` ثم `vercel dev`.
